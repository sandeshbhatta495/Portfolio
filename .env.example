# Flask Configuration
# Copy this file to .env and fill in with your actual values
# IMPORTANT: .env should NEVER be committed to GitHub

# SECRET KEY - Generate a random string for production
# You can generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-this-in-production

# Email Configuration (Gmail SMTP)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# Recipient for contact form submissions
RECIPIENT_EMAIL=your-email@gmail.com

# Flask Environment
FLASK_ENV=development
FLASK_DEBUG=False

# Port (for local development)
PORT=5000

# ============================================================================
# IMPORTANT SETUP INSTRUCTIONS
# ============================================================================

# 1. GMAIL SETUP (if using Gmail for contact form emails):
#    a. Enable 2-Factor Authentication on your Gmail account
#    b. Go to myaccount.google.com/apppasswords
#    c. Create an app password for "Mail"
#    d. Copy the 16-character password and paste above in MAIL_PASSWORD
#    e. Use your full email address in MAIL_USERNAME
#
#    NOTE: Do NOT use your regular Gmail password!
#    The app-specific password is more secure.

# 2. FOR LOCAL DEVELOPMENT:
#    - Copy this file to .env
#    - Fill in the email settings above
#    - Run: source env/bin/activate (or env\Scripts\activate on Windows)
#    - Run: pip install -r requirements.txt
#    - Run: python app.py

# 3. FOR RENDER DEPLOYMENT:
#    - Create variables on Render Dashboard for your Web Service
#    - Set each variable name and value in the Render dashboard
#    - Do NOT upload .env file to GitHub
#    - Render will use the environment variables you set in the dashboard

# 4. GENERATING A SECURE SECRET_KEY:
#    In Python:
#    import secrets
#    print(secrets.token_hex(32))
#    Then copy the output to SECRET_KEY above

# 5. SECURITY CHECKLIST:
#    [ ] Never commit .env to GitHub
#    [ ] Never hardcode email addresses in app code
#    [ ] Never hardcode passwords in app code
#    [ ] Use environment variables for all secrets
#    [ ] Rotate SECRET_KEY when deploying to production
#    [ ] Use app-specific passwords for Gmail, not your regular password
